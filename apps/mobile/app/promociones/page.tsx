'use client';import Link from 'next/link';import {MobileShell} from '@/components/MobileShell';import {useClient} from '@/components/ClientProvider';import {Icon} from '@/components/Icon';
export default function PromotionsPage(){const {data}=useClient();return <MobileShell title="Promociones" eyebrow="BENEFICIOS · HAUT"><div className="page-intro"><span className="eyebrow">EXCLUSIVO EN HAUT</span><h2>Momentos para consentirte.</h2><p>Conoce los beneficios vigentes que nuestro equipo ha preparado para ti.</p></div>{data?.promotions.length?<div className="promo-list">{data.promotions.map(p=><article key={p.id} className="promo-card">{p.image_url?<img src={p.image_url} alt={p.title}/>:<div className="promo-card-no-photo"><Icon name="sparkles" size={27}/></div>}<div className="promo-card-content"><span className="eyebrow">PROMOCIÓN · HAUT</span><h3>{p.title}</h3>{p.description&&<p>{p.description}</p>}{p.treatment_id&&<Link className="secondary-button" href={`/tratamientos/${p.treatment_id}`}>Conocer tratamiento <Icon name="arrow" size={17}/></Link>}</div></article>)}</div>:<div className="empty-feature"><div className="empty-feature-icon"><Icon name="gift" size={28}/></div><h3>Próximamente, más para ti.</h3><p>Cuando HAUT publique nuevas promociones, podrás encontrarlas aquí.</p></div>}</MobileShell>}
