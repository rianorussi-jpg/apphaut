'use client';import Link from 'next/link';import {MobileShell} from '@/components/MobileShell';import {useClient} from '@/components/ClientProvider';
export default function PromotionsPage(){const {data}=useClient();return <MobileShell title="Promociones"><p className="subtle">Beneficios disponibles actualmente en Haut Clinical.</p>{data?.promotions.length?data.promotions.map(p=><article key={p.id} className="card">{p.image_url&&<img className="treatment-photo" src={p.image_url} alt={p.title}/>}<h3>{p.title}</h3><p>{p.description}</p>{p.treatment_id&&<Link className="secondary-button" href={`/tratamientos/${p.treatment_id}`}>Ver tratamiento</Link>}</article>):<div className="empty-card">Por ahora no hay promociones publicadas.</div>}</MobileShell>}
