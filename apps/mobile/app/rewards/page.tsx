'use client';import {MobileShell} from '@/components/MobileShell';import {useClient} from '@/components/ClientProvider';import {dateLabel,pointsBalance} from '@/lib/client-data';
export default function RewardsPage(){const {data}=useClient();return <MobileShell title="Mis rewards"><section className="hero-card" style={{minHeight:160}}><span className="eyebrow">Tus puntos Haut</span><h2>{data?pointsBalance(data):'—'} puntos</h2><p>Tu saldo actual de recompensas.</p></section>
 <div className="section-title"><h2>Movimientos</h2></div>{data?.rewards.length?data.rewards.map(m=><article className="reward-item" key={m.id}><div><strong>{m.description}</strong><p className="subtle">{dateLabel(m.created_at,true)}</p></div><strong className={m.points>0?'reward-positive':'reward-negative'}>{m.points>0?'+':''}{m.points}</strong></article>):<div className="empty-card">Aún no tienes movimientos de rewards. Tus puntos aparecerán aquí cuando Haut los registre.</div>}</MobileShell>}
