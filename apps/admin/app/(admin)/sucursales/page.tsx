"use client";
import {FormEvent,useEffect,useState} from 'react';
import {supabase} from '../../../lib/supabase';
type Branch={id:string;name:string;address:string|null;phone:string|null;is_active:boolean}; type Cabin={id:string;branch_id:string;name:string;is_active:boolean};
export default function SucursalesPage(){const[branches,setBranches]=useState<Branch[]>([]);const[cabins,setCabins]=useState<Cabin[]>([]);const[drafts,setDrafts]=useState<Record<string,string>>({});const[loading,setLoading]=useState(true);const[saving,setSaving]=useState('');const[error,setError]=useState('');const[success,setSuccess]=useState('');
 async function load(){if(!supabase)return;setLoading(true);try{const[b,c]=await Promise.all([supabase.from('branches').select('id,name,address,phone,is_active').order('name'),supabase.from('cabins').select('id,branch_id,name,is_active').order('sort_order')]);if(b.error)throw b.error;if(c.error)throw c.error;const rows=(b.data??[]) as Branch[];setBranches(rows);setDrafts(Object.fromEntries(rows.map(x=>[x.id,x.phone??''])));setCabins((c.data??[])as Cabin[])}catch(e){setError(e instanceof Error?e.message:'No se pudieron cargar las sucursales.')}finally{setLoading(false)}}
 useEffect(()=>{void load()},[]);
 async function savePhone(e:FormEvent,branch:Branch){e.preventDefault();if(!supabase)return;setSaving(branch.id);setError('');setSuccess('');const{error:err}=await supabase.rpc('admin_update_branch_contact',{p_branch_id:branch.id,p_phone:drafts[branch.id]??''});setSaving('');if(err){setError(err.message);return;}setSuccess(`WhatsApp de ${branch.name} actualizado.`);await load();}
 return <section className="page-stack"><div className="page-heading"><p className="eyebrow">Operación</p><h2>Sucursales y cabinas</h2><p className="muted">Configura el contacto de WhatsApp que usará cada cliente para solicitar su siguiente cita.</p></div>{error&&<div className="alert error-alert">{error}</div>}{success&&<div className="alert">{success}</div>}{loading?<div className="fullscreen-inline"><div className="spinner"/></div>:branches.length?<div className="card-grid three-cols">{branches.map(b=>{const branchCabins=cabins.filter(c=>c.branch_id===b.id);return <article className="panel-card branch-card" key={b.id}><div className="section-heading"><div><p className="eyebrow">{b.is_active?'Activa':'Inactiva'}</p><h3>{b.name}</h3></div><span className="count-badge">{branchCabins.length}</span></div><p className="muted">{b.address||'Dirección pendiente'}</p><form className="branch-contact-form" onSubmit={e=>void savePhone(e,b)}><label>WhatsApp de la sucursal<input value={drafts[b.id]??''} onChange={e=>setDrafts(current=>({...current,[b.id]:e.target.value}))} placeholder="+52 442 000 0000" inputMode="tel"/><small>Este número se usa cuando un cliente solicita su próxima sesión desde la app.</small></label><button className="secondary-button inline-button" disabled={saving===b.id}>{saving===b.id?'Guardando…':'Guardar WhatsApp'}</button></form><div className="cabin-list">{branchCabins.length?branchCabins.map(c=><span key={c.id}>{c.name}</span>):<small>Sin cabinas registradas</small>}</div></article>})}</div>:<div className="empty-state large-empty"><strong>No hay sucursales registradas.</strong><span>Agrega las sucursales reales en Supabase.</span></div>}</section>}
