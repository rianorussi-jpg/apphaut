"use client";

import { useEffect, useState } from 'react';
import { supabase } from '../../../lib/supabase';

type Branch={id:string;name:string;address:string|null;phone:string|null;is_active:boolean}; type Cabin={id:string;branch_id:string;name:string;is_active:boolean};
export default function SucursalesPage(){const[branches,setBranches]=useState<Branch[]>([]);const[cabins,setCabins]=useState<Cabin[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{(async()=>{if(!supabase)return;try{const[b,c]=await Promise.all([supabase.from('branches').select('id,name,address,phone,is_active').order('name'),supabase.from('cabins').select('id,branch_id,name,is_active').order('sort_order')]);if(b.error)throw b.error;if(c.error)throw c.error;setBranches((b.data??[])as Branch[]);setCabins((c.data??[])as Cabin[])}catch(e){setError(e instanceof Error?e.message:'No se pudieron cargar las sucursales.')}finally{setLoading(false)}})()},[]);return <section className="page-stack"><div className="page-heading"><p className="eyebrow">Operación</p><h2>Sucursales y cabinas</h2><p className="muted">Las cabinas se renderizan dinámicamente; no hay tres columnas hardcodeadas.</p></div>{error&&<div className="alert error-alert">{error}</div>}{loading?<div className="fullscreen-inline"><div className="spinner"/></div>:branches.length?<div className="card-grid three-cols">{branches.map(b=>{const branchCabins=cabins.filter(c=>c.branch_id===b.id);return <article className="panel-card branch-card" key={b.id}><div className="section-heading"><div><p className="eyebrow">{b.is_active?'Activa':'Inactiva'}</p><h3>{b.name}</h3></div><span className="count-badge">{branchCabins.length}</span></div><p className="muted">{b.address||'Dirección pendiente'}</p>{b.phone&&<p className="muted">{b.phone}</p>}<div className="cabin-list">{branchCabins.length?branchCabins.map(c=><span key={c.id}>{c.name}</span>):<small>Sin cabinas registradas</small>}</div></article>})}</div>:<div className="empty-state large-empty"><strong>No hay sucursales registradas.</strong><span>El seed no crea sucursales ficticias; agrega las reales en Supabase.</span></div>}</section>}
